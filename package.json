{
  "devDependencies": {
    "mermaid": "^10.9.1",
    "vitepress": "^1.2.3",
    "vitepress-plugin-mermaid": "^2.0.16"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "vercel-build": "chmod +x ./node_modules/.bin/vitepress && npm run docs:build"
  },
  "dependencies": {
    "markdown-it": "^14.1.0"
  }
}